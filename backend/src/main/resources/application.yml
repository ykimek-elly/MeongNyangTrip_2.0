spring:
  config:
    import: optional:file:./.env[.properties]

  application:
    name: meongnyang-backend

  # 활성 프로필 (로컬 개발: dev, 운영: prod)
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

  # Gemini
  ai:
    google:
      genai:
        api-key: ${GEMINI_API_KEY}
        embedding:
          api-key: ${GEMINI_API_KEY}
          text:
            options:
              model: gemini-embedding-001
        chat:
          options:
            model: gemini-2.5-flash-lite
            temperature: 0.7
  # JDK 21 Virtual Threads 활성화
  threads:
    virtual:
      enabled: true

  # PostgreSQL + PostGIS 연결
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:meongnyang}
    username: ${DB_USERNAME:meongnyang}
    password: ${DB_PASSWORD:meongnyang1234}
    driver-class-name: org.postgresql.Driver

  # JPA 설정
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:update}
    show-sql: ${JPA_SHOW_SQL:true}
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  # Redis 연결
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

server:
  port: ${SERVER_PORT:8080}

# 한국관광공사 반려동물 동반여행 API
pet-tour:
  service-key: ${PET_TOUR_SERVICE_KEY:}
  base-url: https://apis.data.go.kr/B551011/KorPetTourService2

# 카카오 REST API (Local API 교차검증)
kakao:
  rest-api-key: ${KAKAO_REST_API_KEY:}

# Google Places API (3단계 속성 보강 — allowsDogs)
google:
  places:
    api-key: ${GOOGLE_PLACES_API_KEY:}
    base-url: https://places.googleapis.com/v1

# 네이버 지역 검색 API (이미지 보강 — imageUrl 없는 장소 대상)
# 발급: https://developers.naver.com → 앱 등록 → "검색 > 지역" 권한 추가
naver:
  local:
    client-id: ${NAVER_LOCAL_CLIENT_ID:}
    client-secret: ${NAVER_LOCAL_CLIENT_SECRET:}

# JWT 설정 (팀원 A 작업용)
jwt:
  secret: ${JWT_SECRET:dev-secret-key-change-in-production}
  expiration: ${JWT_EXPIRATION:3600000}

weather:
  api:
    key: ${WEATHER_API_KEY:}

# ==========================================
# 📌 팀원 별 추가 설정 가이드 (작업 전 확인!)
# ==========================================
# TODO (팀원 A): 배포 시 application-prod.yml 파일 분리 및 AWS S3 설정 추가 필요
# TODO (팀원 B): Redis 캐시 만료 시간 및 Connection Pool 세부 설정 필요 시 여기에 추가
# TODO (팀원 C): Spring AI (Gemini) 연동을 위한 spring.ai.openai.api-key 설정 추가 필요

